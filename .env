# Leave empty so the browser calls /api on the same host (Vite proxies to the backend).
# VITE_API_URL=

# Backend URL used by the Vite dev/preview server proxy (not exposed to the browser).
# Local backend on the same machine:
# VITE_PROXY_TARGET=http://127.0.0.1:3000

# Production: backend EC2 (server-side proxy avoids browser timeouts to port 3000)
VITE_PROXY_TARGET=http://13.63.159.87:3000
# VITE_PROXY_TARGET=http://localhost:3000